# Netlify SPA Redirects
# Static assets are served as-is by Netlify (they take priority over rewrites)
# Only non-file routes are rewritten to index.html for SPA routing
/app        /index.html   200
/app/*      /index.html   200
